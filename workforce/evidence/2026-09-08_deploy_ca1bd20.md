# Nachweis: Deploy `ca1bd20` auf die NAS, 2026-09-08

**Freigabe:** CEO im Chat, Klasse Produktives, 2026-09-08 (`CEO-CHAT-2026-09-08/PENDING-DEC`):
„Der CEO gibt den Deploy des Standes ca1bd20 auf die NAS über deploy_nas.sh frei. Danach verify
im Container und Gerds Review nach dem Lauf." Gerds Vorab-Freigabe: `226f0b9` deploybar über
`deploy_nas.sh` aus sauberem Baum; `ca1bd20` liegt drei Dokumentationscommits danach.

**Ausführung (Karl, Claude Code, ca. 08:30 CEST):** `HEAD` des Repos stand bereits auf
`b299ec2` (Parallelsitzung, nur Skill- und Gedächtnisänderungen). Deshalb Wegwerf-Klon
(`git clone --shared`, `git checkout ca1bd20`, `git status --porcelain` leer), Konfiguration und
beide Secrets hineinkopiert (`600`), `sh workforce/deploy_nas.sh`. Vorher `max_usd_per_day` in
`config.nas.json` von 1.0 auf 2.0 gesetzt (CEO, Klasse Budget, derselbe Chat). Klon samt
Secret-Kopien danach gelöscht, Abwesenheit mit `test ! -e` geprüft.

Ausgabe des Skripts (Ende):

```
Container workforce-workforce-1  Recreated
Container workforce-workforce-1  Started
RESULT: deployed ca1bd20
```

**Messung im Container** (`docker compose -f compose.yaml -f compose.claude.yaml exec -T workforce …`):

```
verify:  Audit: intakt / RESULT: PASS / exit 0
status:  Kanal: ACTIVE / Heute: 0 Aufrufe, 0.0000 USD von 2.00 / Offen: 0 Ausgaenge / Audit: intakt
/run/secrets: -rw-r----- 1026:workforce anthropic_api_key, telegram_bot_token
log:     workforce: Kanal ACTIVE, Standardidentitaet KARL
```

**Nicht gemessen:** ob nach dem Neustart eine echte Telegram-Nachricht beantwortet wird; das
ist der nächste Kontakt des CEO mit dem Bot. Kanal `ACTIVE` nach Neustart ist persistierter
Zustand aus der Zustandsdatei, kein frischer Start; Gerd prüft im Review nach dem Lauf, ob das
zu Invariante 1 passt.
